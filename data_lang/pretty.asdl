# Pretty printing "documents". A `doc` encodes printing instructions, including
# choices between multiple possible layouts.

module pretty
{

doc =
    Newline
  | Text(str string)
  | Indent(int indent, doc node)
  | Concat(List[doc] nodes)

DocFragment = (doc node, int indent, bool flat)

}
