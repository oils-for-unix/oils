`vendor/` Directory
===================

- `typing.py` is stripped-down version of the Python stdlib module.  This is
  the bare minimum so that imports don't fail in the `ovm-tarball` build.
- `ninja_syntax.py` is a file from the ninja repo that we're using for
  "metaprogramming".
