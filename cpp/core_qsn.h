// core_qsn.h

#ifndef CORE_QSN_H
#define CORE_QSN_H

#include "mylib.h"

namespace qsn {

bool IsUnprintableLow(Str* ch) {
  assert(0);
}

bool IsUnprintableHigh(Str* ch) {
  assert(0);
}

bool IsPlainChar(Str* ch) {
  assert(0);
}

Str* XEscape(Str* ch) {
  assert(0);
}
 
}  // namespace qsn

#endif  // CORE_QSN_H

