#!/usr/bin/env ysh

module stdlib/random || return 0

func randomInt(byteSize = 4) { # defaults to 32-bit ints
  var ret = $(od -vAn -N$byteSize -tu$byteSize < /dev/urandom | tr -d '[:space:]') => int()
  return (ret)
}

